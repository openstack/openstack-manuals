---
additionalProperties: false
type: array
items:
  type: object
  required:
    - name
    - service
    - type
  properties:
    name:
      type: string
    service:
      type: string
    type:
      type: string
      enum: ["service", "client", "library", "other"]
    description:
      type: string
    has_install_guide:
      type: boolean
